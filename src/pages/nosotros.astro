---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import About from '../components/About.astro';
import Footer from '../components/Footer.astro';
import { getSiteSettings } from '../lib/sanity';

const settings = await getSiteSettings();
const businessName = settings?.businessName ?? 'Agencia de Turismo';
---

<Layout title={`Nosotros — ${businessName}`} description={`Conoce la historia de ${businessName}, nuestro equipo de guías certificados y la historia detrás de ${businessName}.`}>
  <Navbar />
  <main class="pt-16">
    <About />
  </main>
  <Footer />
</Layout>
